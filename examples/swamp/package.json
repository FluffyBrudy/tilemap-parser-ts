{
  "name": "sporeling-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sporeling game ported to Phaser on tilemap-parser-ts (full playable, straight to gameplay). Excluded from packaging.",
  "scripts": {
    "dev": "vite --port 5210",
    "build": "vite build",
    "preview": "vite preview --port 5210",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tilemap-parser-ts": "file:../..",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "^6.0.0"
  }
}
