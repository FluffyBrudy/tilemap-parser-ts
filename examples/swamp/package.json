{
  "name": "swamp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Swamp game ported to Phaser on tilemap-parser (full playable, straight to gameplay). Excluded from packaging.",
  "scripts": {
    "dev": "vite --port 5210",
    "build": "vite build",
    "preview": "vite preview --port 5210",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tilemap-parser": "file:../..",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "^6.0.0"
  }
}
