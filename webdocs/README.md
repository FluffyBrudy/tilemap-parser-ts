# webdocs (reserved)

This directory is reserved for a future standalone docs website (own small
project, deployed on Vercel). It intentionally contains no source yet and is
excluded from the npm tarball (`files` allowlist), `tsconfig.json`, and
`eslint.config.js`.
